# Copy to .env and fill in from your Supabase project's Settings → API.
# The anon key is safe for client apps — RLS policies gate all data access.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
